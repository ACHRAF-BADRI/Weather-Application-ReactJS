# Copy to .env.local for local development.
# This is NOT a secret: it is just the address of the back-end (server/).
# The WeatherAPI key lives only in server/.env.
NEXT_PUBLIC_API_URL=http://localhost:4000
